/**
 * Shared cross-cutting concerns: security, exception handling, configuration. All domain modules
 * may depend on shared; shared must NOT depend on domain modules.
 */
package com.conduit.shared;
